import { lightColors } from './colors'

export const lightTheme = {
  colors: lightColors,
  headings: {
    sme: {
      fontSize: 14,
      fontWeight: 700,
    },
    sm: {
      fontSize: 14,
      fontWeight: 600,
    },
  },
  shadows: {
    menuShadow: '0 10px 20px rgba(0, 0, 0, .1)',
  },
}
