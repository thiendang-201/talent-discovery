import { lightColors } from './colors'

export const lightTheme = {
  colors: lightColors,
  headings: {
    sme: {
      fontSize: 14,
      fontWeight: 700,
    },
    sm: {
      fontSize: 14,
      fontWeight: 600,
    },
  },
}
